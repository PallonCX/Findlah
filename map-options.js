(function(window, google, mapster){
    
    mapster.MAP_OPTIONS = {
        center: {
            lat: 0,
            lng: 0
        },
        geocoder = true;
    }

}(window, google, window.Mapster || (window.Mapster = {})))
